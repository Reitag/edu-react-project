import { Layout } from "./Layout";
import { RestaurantTabs } from "./RestaurantTabs/RestaurantTabs";

export const App = () => {
  return (
    <Layout>
      <RestaurantTabs />
    </Layout>
  );
};
